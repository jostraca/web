# build output
dist/
.astro/

# dependencies
node_modules/

# worker-configuration.d.ts is generated by `npm run cf-typegen` and IS
# committed: `tsc` needs the Env type, and a contributor who has not run
# wrangler should still be able to typecheck a clone. Rerun cf-typegen after
# editing wrangler.json.

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS
.DS_Store

# wrangler
.wrangler/
